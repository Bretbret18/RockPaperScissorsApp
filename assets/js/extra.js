// lines 311 to 317 incase if needed
 // let winResult = roundResults[0].playerWin;
                // let loseResult = roundResults[1].opponentWin;
                // let drawResult = roundResults[2].draw;
                // if (winResult == true || loseResult == true || drawResult == true) {
                //     const gameStats = `{"Win":${roundResults[0].playerWin}, "Lose":${roundResults[1].opponentWin}, "Draw":${roundResults[2].draw}}`;
                //     console.log(JSON.parse(gameStats))
                // }